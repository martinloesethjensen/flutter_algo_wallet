import 'package:flutter/material.dart';

class LoadedWalletWidget extends StatelessWidget {
  const LoadedWalletWidget();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Loaded Wallet Screen"),
      ),
    );
  }
}
